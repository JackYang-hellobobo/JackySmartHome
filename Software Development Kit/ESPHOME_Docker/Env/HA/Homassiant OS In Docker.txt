docker run -d \
  --name esphome \
  --privileged \
  --env HTTPS_PROXY="127.0.0.1:7893" \
  --restart=unless-stopped \
  -e TZ=MY_TIME_ZONE \
  -v /ESPHOME:/config \
  -v /run/dbus:/run/dbus:ro \
  --network=host \
  ghcr.io/esphome/esphome

